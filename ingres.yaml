apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zec-timing-ingress
spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx-gateway-service
                port:
                  number: 80
